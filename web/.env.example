# Stytch PUBLIC token (safe to ship in frontend) — from Stytch dashboard → API Keys.
# This is NOT the secret; it's the public_token used by the browser SDK.
VITE_STYTCH_PUBLIC_TOKEN=public-token-test-xxxxxxxx
